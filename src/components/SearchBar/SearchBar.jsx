// Text Input
// Filter Button
// Preview Pane
// Filter and Clear Logic

// (onclick dismiss search and scroll to message + highlight)
// (show results pane on top of messages when search bar is active, else hide pane)
// (cancel button to clear search field and close results pane)
// (x button to clear search field only while remaining active - results field shown)
// (results pane to show sender name (or 'you'), avatar, messages, time only, with sticky headers to show date only)
